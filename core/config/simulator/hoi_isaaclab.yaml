# @package _global_

defaults:
  - base
  - physx_isaaclab

simulator:
  _target_: core.simulator.hoi_isaaclab.simulator.IsaacLabSimulator
  _config_target_: core.simulator.hoi_isaaclab.config.IsaacLabSimulatorConfig
  config:
    w_last: false  # IsaacLab is xyzw quaternions
    sim:
      fps: ${robot.sim.isaaclab.fps}
      decimation: ${robot.sim.isaaclab.decimation}
